{
  "name": "skoschool9-edu",
  "private": false,
  "version": "1.0.0",
  "type": "module",
  "homepage": "https://zdanial-44.github.io/School_9_SKO/",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && mkdir temp_dist && cp -r dist/* temp_dist && gh-pages -d temp_dist && rm -rf temp_dist"
  },
  "dependencies": {
    "router": "^2.2.0",
    "vue": "^3.5.18",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.0",
    "gh-pages": "^6.3.0",
    "vite": "^7.0.6"
  }
}
